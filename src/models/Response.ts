interface ResponseData {
    code: any;
    status: any;
    message: any; // Using `any` as it's an `object` in C# and can be anything
    data: any; // Using `any` as it's an `object` in C# and can be anything
}